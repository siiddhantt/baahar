The preview is structurally close but contract-invalid. Keep the official BIC JSON request, top-level events loop, stable Tribe id, source URLs, UTC/local dates, venue, null preservation, image fallback, and all 27 exact keys. Fix only these contract facts: schema_version must be literal "event-occurrence/v1"; time_precision must be "timed" when all_day is false and "date" when true (date records require starts_at and ends_at null); category must use the documented precedence and map Business plus the known civic/science topic categories to "talks". Decoding Behavioural Insights has Business and must preview as talks. Allowed categories: arts,talks,theatre,music,books,community,other. Unknown price/free/currency/registration/age/accessibility stay null and language stays []. Do not infer from title or description. Preview at least the Business record.
