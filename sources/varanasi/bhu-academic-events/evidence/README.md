# BHU Academic Events evidence

One Bright Data collector exists. Its generated multi-stage Browser draft was
rejected, the reviewed single-stage Code worker and authoritative schema were
installed, and the saved development template was deliberately promoted to
Production. One explicit production batch passed. A separately gated
application activation then stored and published a second exact collection;
the source is active and Varanasi is enabled in the public API.

## Bright Data create gate — 19 August 2026

Exactly one authenticated external create was issued with official
`@brightdata/cli` 0.3.5 and `--no-retry` from the tracked
`../collector/create-prompt.txt`:

- collector ID: `c_mszvpbm220j1pld0pe`;
- name: `baahar-bhu-academic-events`;
- terminal status: `done` after 585 polls;
- created: `2026-08-19T09:18:01.226Z`;
- view: `https://brightdata.com/cp/scrapers/c_mszvpbm220j1pld0pe`;
- private immutable create envelope: 393 bytes;
- envelope SHA-256:
  `fd5bba1968b82264d858ac234b6fc7ae9b7ec04aec895c2c18c61579b2907324`.

The first local CLI process stopped before authentication with `No API key
found`; it created no template, envelope, or external state. The one
authenticated create above is the sole BHU collector creation. The generated
two-stage Browser/fan-out draft was rejected and was never approved as source
proof.

## Local live gate — 19 August 2026

The corrected focused harness passed 6/6 tests against the current official
API without a browser or session bootstrap:

- one direct public POST with the reviewed URL, JSON content type, and
  byte-exact request body;
- API response: HTTP 200, 1,778,717 UTF-8 bytes, SHA-256
  `65cd9f28bb4fd08beb1f7e3ea19a0b085bcf9fffdeea6edd9c3ca8ad99ba082b`,
  756 rows, and 20 source-declared Upcoming rows;
- Code-worker path: one request, zero navigation, parser, cookie, action, or
  fan-out calls;
- emitted current boundary: 10 rows with native IDs, `OpenTo=All`, explicit
  Varanasi locations, and starts inside the 90-day local-calendar horizon;
- canonical rows: exact 27 keys, schema-valid, zero duplicate IDs, one
  explicitly supported registration URL, and SHA-256
  `a6f3002c04052d13342ba1a7de939c7af025ad29bf4a3fbbd0010a33469c56bd`;
- mutation gates proved malformed dates, partial times, numeric/object time
  fields, duplicate IDs, ambiguous registration links, low counts, wrong
  input, and horizon drift fail before collection;
- `node --check`, strict tracked-manifest validation, and Prettier passed.

The API byte hash is evidence for the dated fetch, not a frozen baseline.
Completeness and semantic gates use the exact response shape and canonical
record set.

## Reviewed Studio installation and preview — 19 August 2026

The signed-in editor was reduced to one Code stage with the exact tracked
worker, an empty parser, one canonical input, and the authoritative 27-field
schema. The generated second stage was deleted with confirmation. A normalized
editor copy matched the tracked worker byte-for-byte; its SHA-256 was
`bc502740254b4da406a056ce1df0b89586b5d89f43b452fe30b42c14fad0fc74`.

The save-to-development probe completed with one input, one documented POST,
10 output rows, and no parser or fan-out. The separately reviewed explicit
preview was:

- preview: `preview_mszwwank2e5ytaiyya`;
- input: the exact manifest URL;
- output: 10 rows with the exact 27-field schema;
- downloaded output: 8,998 bytes;
- downloaded SHA-256:
  `beb9da4afa07f67e5f5a26a1161615324e158d7bb571b32897a9310c2f05e829`;
- native IDs: `6386`, `6383`, `6382`, `6389`, `6385`, `6381`, `6397`,
  `6396`, `6376`, and `6387`.

All 10 preview rows passed the authoritative Go schema and focused semantic
gate. They were timed, scheduled, source-explicit Varanasi occurrences; nine
had no registration URL and ID `6381` had the single reviewed `forms.gle`
registration URL. The development template was then promoted to Production on
the same collector. No duplicate collector, generated repair, heal, or preview
retry was used.

## Immutable production batch — 19 August 2026

- collection: `j_mszx45ss2cqoprf9fr`;
- trigger response: HTTP 200;
- trigger request: 70 bytes, SHA-256
  `b661003ec7788cb1ccf0b68cd8f47ab2edb68ac56707f3fc09fb1f16e72fa081`;
- trigger response: 79 bytes, SHA-256
  `cd9a490bfc57bc059804b9ba5730675b5e5d9121049009622c9a9ce413060e0`;
- trigger start ETA: `2026-08-19T09:57:33.292Z`;
- dataset polls: HTTP 202, 202, then 200;
- pending response: 55 bytes, SHA-256
  `ff2f8ff073c6aaf37e2601dde6c32c5add156123847cb54ac04ad7a5791d2210`;
- records: 10;
- raw dataset: 11,639 bytes, SHA-256
  `5bb9967a3b847e09ccce18544b7a8ff14ad94eac2ff087416929a84adf054c98`.

Every raw row has one uniform 28-key transport shape: the 27 canonical fields
plus Bright Data's `input` member. Every `input` is exactly the one-member
manifest object and exact official URL. All reviewed null fields were retained;
the raw transport did not drop nullable keys.

Removing only the verified transport `input` member in memory produced an
8,998-byte 27-field validation view with SHA-256
`395b77876fe1c023c9913a882213b074755543de64641f63b86dc40d00f1e043`.
All 10 records passed the authoritative Go Draft 2020-12 validator and the
source semantic gate with zero errors: exact expected IDs, unique identities,
official detail URLs and hosts, Varanasi venues, ordered timed boundaries,
matching local dates, scheduled status, Asia/Kolkata timezone, and the single
reviewed registration URL. Excluding the run-specific `observed_at`, the
production facts are deeply equal to the reviewed preview facts.

The exact trigger request, trigger response, pending poll responses, immutable
raw dataset, and derived validation view are stored separately in the ignored
private evidence directory. There was one source-lane trigger, no retry, no
heal, and no backend invocation during this collector-verification gate.

## Application activation and publication — 19 August 2026

The backend activated this source exactly once in run
`01a01985-ba89-72a0-aac6-f3d5a29461ee` using collection
`j_mszxuax02b3txarda`. It stored 11,639 immutable bytes with SHA-256
`22493a5d63bf9c7e489ba2b8a2581b58219d17431a65ba4f09a16bf34394c8af`
and published 10/10 records with zero quarantine. The published native IDs are
`6376`, `6381`, `6382`, `6383`, `6385`, `6386`, `6387`, `6389`, `6396`, and
`6397`; only `6381` carries the reviewed `forms.gle` registration URL.

The public cities, feed, detail, ICS, and source-summary checks passed. BHU is
active and Varanasi is enabled. This activation did not collect BIC or Jagriti.

## Workshops taxonomy deployment check — 19 August 2026

The tracked worker adds the source-explicit `workshops` category without
changing the request, record set, identity, dates, URLs, or nullable fields.
Two human-reviewed same-ID Self-Healing attempts were kept outside Production.
Both proposed previews sampled only IDs `6386` and `6383`; both classified the
sample workshop correctly, but that sample was insufficient to approve the
required full-batch invariant.

The approved development-only results were therefore run explicitly before any
Production save:

- development v4, collection `j_mt02nubqjkwp0n1u6`, 10 rows, 11,639 bytes,
  SHA-256 `09bce5edc13ea108ab4e9b724bd9b5d67bceee5649823b49fb61abb8a61cf9fa`;
- development v5, collection `j_mt02w8fafouf9n1cn`, 10 rows, 11,639 bytes,
  SHA-256 `cf34edc2ad537cf7147ecfbba0111004437fbdf4fbeb024237e052c78026f11a`.

Both full development datasets retained the 10 reviewed IDs and transport
shape but still emitted the old `8 talks / 2 other / 0 workshops` mapping. The
repair results were rejected and neither reached Production. The correction was
subsequently completed by installing the exact tracked classifier and matching
canonical-record allowlist described below.

## Workshops taxonomy publication — 20 August 2026

The corrected development v6 run `j_mt0m5gqs1bc7d1hh5h` completed with one
input, one page, 10 records, zero failed crawls, and no schema errors. Its
immutable dataset is 11,655 bytes with SHA-256
`ad49d2c35724c8ca04bcce56eee9e99b7f767ac479dfe8f76d189e443dee9b80`.
Every row had the reviewed 28-key Bright transport shape; removing only the
exact manifest `input` envelope yielded 10 exact 27-field records. All 10
native IDs were unchanged and the complete taxonomy was exactly four
`workshops`, four `talks`, and two `other` records. That development version
was reviewed and saved to Production on the same Collector ID.

The protected application run `01a01bf9-16f3-7368-830a-bd438044d4f6`
created exactly one Bright collection, `j_mt0mai5322gfe9eio9`, and published
10/10 records with zero quarantine. The stored object is 11,655 bytes with
SHA-256
`582c87103e1ebfa09e95ed111a32d0dd492bcf6df2d8079f74d70629e090b38a`;
database metadata, MinIO metadata, streamed bytes, and streamed hash matched.
Only the four recategorized occurrences gained new versions, and no public
change-history notices were invented. BIC and Jagriti remained at their exact
prior collection counts, occurrence/version counts, and due times.

The live API returns 10 Varanasi plans split `4 workshops / 4 talks / 2 other`.
The real Baahar route
`/varanasi?window=upcoming&category=workshops` renders four cards; workshop
detail navigation preserves the exact filtered back link.
